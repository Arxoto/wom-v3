# 打字到检索的时序

Type: grilling
Status: resolved

## Question

打字怎么变成检索？合成期间哪些键放行？怎么防止响应乱序？按住方向键的连发怎么办？

## Answer

- **合成期间**：input 路径由 [IME 合成期间的事件语义](01-research-ime-composition.md) 定——`compositionstart` / `compositionend` 的 lock 挡住防抖与检索，`compositionend` 自己补一次检索（靠「文本没变不重发」去重）。keydown 路径暂不做合成判断：按「候选框打开时这些键到不了页面」的假设先实现，功能验收时再实测。按 ESC 取消候选却把窗口关了、按 Enter 提交候选却触发了动作，都是要盯的硬伤。
- **检索时机**：非合成变更后 50ms 尾触发防抖；文本与上次发送相同就不重发（后端另有 `input_key` 缓存兜底）。
- **乱序**：请求令牌只与「当前最新令牌」比相等，**不比较大小**；令牌用有界环计数器（如 `% 256`，远大于同时在飞的请求数），数字不会无限增长，回绕也不会误判。
- **连发**：按住 ↑/↓ 限流约 100ms（写成常量）；触控板的惯性滚动不在此列。

## Comments

- 2026-09-19（依代码订正）：下面几条阶段机的评论停在「三阶段状态机」那一版；`b7dc7c4` 之后阶段整个拿掉了——`search_session.ts` 只剩 `last_sent` / `search_token` / `prefetch_in_flight` 三笔记账加防抖与合成锁，翻页判据不再看阶段（判据与后果见 [spec.md](../spec.md) §2 / §3）。Answer 那四条（合成期间、检索时机、乱序、连发）与现状一致。
- 2026-09-19：可读性整理——每一次阶段流转都收进一个入场处理（`enter_typing` / `enter_searching` / `enter_settled`），`advance_phase(事件)` 先由 `next_phase(事件)` 算目标阶段、再交给该阶段的入场处理，`phase` 全文件只有这一处被赋值。入场处理自己声明并清掉该阶段的账：进输入阶段与完成阶段都作废上一笔排上的防抖（它带的是被这次输入取代的旧文本），进搜索阶段没有要清的账（排上那一笔就是这一笔，已经销掉）。答复到期抽成 `reply_arrived`，成功与失败走同一条路（原先 `.catch` 里与主线重复的那份「判过期 + 判阶段 + 销账」收掉了）；两个在飞槽改名 `first_page_in_flight` / `prefetch_in_flight` 并写明归属——前者跟着请求的生死走、不跟着阶段走（关键字没变时输入会回到输入阶段，在飞的那一笔仍是这一轮的答案），所以它不在完成阶段的入场处理里收账，而是由 `reply_arrived` / `invalidate_in_flight` 收。行为不变：同一串随机事件序列（虚拟时钟 + 固定种子，3 组 × 4000 次事件，覆盖输入 / 合成 / 预请求 / 乱序与失败的答复 / 时间推进）打在改前改后的会话上，两侧的可观测输出（发出去的检索与预请求、落定的结论、续上的页）逐行一致；拿掉「只有完成阶段才翻页」那条判据做负对照，同一序列能打出九千多处差异。
- 2026-09-19：阶段从「每次现算」改成**存着的一份状态机**——`search_session.ts` 导出纯函数 `next_phase(事件)`，事件是四个有限的字符串：`input` → 输入阶段（输入动作发生，合成开始也算一次）、`search_pending` → 搜索阶段（有答复在路上：刚发出去，或关键字没变、在飞的那一笔就是答案）、`search_settled` → 完成阶段（这一轮不用等：输入清空当场落定，或到点确认不用发）、`reply` → 完成阶段（答复到达）。事件**不带判据、流转表也不读当前阶段**：该报哪一条、该不该报，都由调用侧按手里的变量挑（这次输入有没有留下一笔要搜的、还有没有答复在路上、还在不在等这一笔——答复到达时先问一句，输入阶段的旧答复只换结论、阶段不动）。**存的理由**是把「这一刻算什么阶段」收成一处；上一版担心的那种漏同步（去重那一支写成完成、而答复还在路上）由「到点报 `search_pending` 还是 `search_settled` 先问过 `awaiting_reply`」挡住。行为不变：只有完成阶段才翻页这一条照旧（黑盒比对：400 组随机事件序列里，翻页判据每一步都与老口径的「现算」一致）。
- 2026-09-19：三阶段状态机落地（改动见 `src/main/interaction/search_session.ts` / `reducer.ts` / `useMainInteraction.ts` / `AppMain.tsx`，口径见 [spec.md](../main-keyboard/spec.md) §2 / §3）。输入 → 检索 这条线改成 `settled` / `typing` / `searching` 三阶段；渲染侧只剩一份「结论」（`MainState.conclusion`，`empty = conclusion === null`），**阶段不进画面**——输入阶段与搜索阶段都保持上一份结论的样子，打字过程中画面一次都不变：首屏第一次输入不再先闪一句「没有匹配的条目」/ `N/A`，上一条结论是 `N/A` 时它稳定留着，直到新结论落定才换。要另给「搜索中」一个样子（比如变淡）的话，得在渲染投影里显式加一条，而不是像 `searching` 那样顺手带出来。`on_search_pending` / `search_pending` / `MainState.searching` 随之删除，`on_page_loaded` 改名 `on_conclusion`（`null` = 这一轮没有查询）。空输入改成**当场落定**（没有要合并的击键，不必等 50ms），上面「那 50ms 防抖不在其中」那句随之作废。合成锁保留为与阶段正交的一把锁：合成期间阶段是输入阶段，只是不排防抖。判断依据与手感留在一个一次性原型里：`.scratch/main-keyboard/prototype/input-phase-states.html`（双击即开，机器部分与落地代码同形）。
- 2026-09-19：评审回修——阶段不另存，由「合成锁着没有 / 防抖排着没有 / 答复在路上没有」三件事**现算**，唯一的读点是翻页判据（只有完成阶段才翻页）。存一份就要在每处同步，漏一处就会出现「说完成、其实还有一笔在路上」的状态：原来的 `send` 去重分支（关键字没变不重发）会把阶段写成完成，而那一笔答复还在路上，翻页判据因此漏过去（复现：发检索 → 补一个空格 → 防抖到点 → 按 ↓，预请求照样发出）。现在那一支**一个字段都不动**，阶段只由三件事的真相推进：发出去挂上「在等答复」，结果落定 / 失败 / 被新输入作废时收掉。顺带修掉一个旧口子：只有完成阶段才发预请求（输入阶段与搜索阶段里列表都还是上一条关键字的结果，翻它的下一页要么被令牌丢掉，要么在新结论落定之后接错到新列表上）。
- 2026-09-19：评审回修（二）——防抖的 handle 记账抽成 `src/main/interaction/debounce.ts`（`create_debounce` → `schedule` / `cancel` / `pending`）：到点先销账再跑、排自带取消这两条标准动作只写一遍，`search_session` 里剩下的都是业务（什么时候排、排什么、「还在防抖窗口里」对阶段意味着什么）。这一拆分也把那句容易读歪的注释从业务文件里拿掉了（「防抖到点时把 handle 放下」本来就是防抖自己的事，不是检索的事）。
- 2026-09-19：`searching` 的口径收窄成「发出去那一刻才挂」——`send` 里 `deps.search` 之前报 `on_search_pending(true)`；结果回来由 reducer 的 `page_loaded` 复位，失败与「到点却没发新的」（令牌已前进、在飞那一笔的响应会被丢弃）由会话补报 `false`。防抖那 50ms 不再算：首屏第一次输入、上一条零结果后再打字、合成结束补的那次，在这 50ms 里照旧显示「`N/A` / 没有匹配的条目」。下面两条里「排上防抖就挂」随之作废。
- 2026-09-19：「搜索中」这一档作废——**搜索期间与空输入是同一副样子**：列表区与右上角标签都留白。判据从两个组件的 props 里收成一个 `empty`，由 `AppMain` 现算（`input === "" || searching`），`Head` / `Body` 各只收这一个；`MainState.searching` 与 `on_search_pending` 保留，它只管「结果还在路上」。下面那条里「搜索中」那一档与标签的说法随之作废。
- 2026-09-19：列表为空时那句占位文案多了一档——结果还没回来时是「搜索中」，落到没匹配才是「没有匹配的条目」；右上角标签同理，结果没回来时不显示 `N/A`（`total` 还是上一条的值），留白，有结果时的「序号 / 总数」照旧。判据是 `MainState.searching`，由 `search_session` 的 `on_search_pending` 报：排上防抖（这次输入真会发检索）或第一页在飞时挂上，落定（结果回来 / 失败 / 这一轮不搜）收掉；空输入的清空那一轮不算。改动前打字到结果回来之间那句文案一律显示「没有匹配的条目」、标签闪 `N/A`，首屏第一次输入就能看到（见 [spec.md](../main-keyboard/spec.md) §2 / §3）。
- 2026-09-19：输入清空到空串改为**不检索**——尾防抖到点后 `send` 判空，不发请求而直接回调 `on_page_loaded(undefined)`，reducer 据此把结果整份清空（列表区提示输入搜索）；在飞的第一页与预请求同时作废、去重记录复位，清空后重打同一个关键字会重新搜。搜不搜与关键字怎么切（第一个空格之前）都在 `send` 里，以空格开头时关键字是空串即全量（见 [spec.md](../main-keyboard/spec.md) §3）。下面那条里「输入清空到空串仍照常检索」随之作废。
- 2026-09-18：首屏不再自动检索——挂载时那次空关键字请求去掉，列表空着直到第一次输入；输入清空到空串仍照常检索。`SearchSession` 的对外方法随之按事件改名：`send` 不再对外（只有内部防抖定时器调它），`schedule` → `input_changed`，`cancel` → `dispose`（见 [spec.md](../main-keyboard/spec.md) §3）。
- 判据在 2026-09-14 收口过一次（原文写的是「合成期间所有被拦截的键一律放行」，那要求 keydown 能判出合成状态）。当前口径见 Answer 第一条。
